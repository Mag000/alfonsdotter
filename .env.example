# Admin password used for the /admin editor (and public site gate when enabled)
VITE_ADMIN_PASSWORD=changeme

# Set to "true" to require admin login before the public site is shown
VITE_PUBLIC_REQUIRES_AUTH=false
